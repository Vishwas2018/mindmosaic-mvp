import { AuthCard } from "@/components/auth/AuthCard";

export const metadata = { title: "Create account — MindMosaic" };

export default function SignupPage() {
  return <AuthCard initialView="sign-up" />;
}
